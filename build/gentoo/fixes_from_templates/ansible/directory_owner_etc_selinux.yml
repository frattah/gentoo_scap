# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low

- name: Ensure owner on directory /etc/selinux/
  file:
    path: "/etc/selinux/"
    state: directory

    owner: "0"



