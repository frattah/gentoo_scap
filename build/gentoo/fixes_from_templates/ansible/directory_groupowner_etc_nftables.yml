# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low

- name: Ensure group owner on /etc/nftables/
  file:
    path: "/etc/nftables/"
    state: directory

    group: "root"



