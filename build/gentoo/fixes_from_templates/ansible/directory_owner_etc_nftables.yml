# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low

- name: Ensure owner on directory /etc/nftables/
  file:
    path: "/etc/nftables/"
    state: directory

    owner: "0"



