# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low

- name: Ensure owner on directory /etc/sysctl.d/
  file:
    path: "/etc/sysctl.d/"
    state: directory

    owner: "0"



