# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure gnome-software is installed
  package:
    name: "gnome-software"
    state: present
