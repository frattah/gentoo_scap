# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low

- name: Ensure owner on directory /etc/ipsec.d/
  file:
    path: "/etc/ipsec.d/"
    state: directory

    owner: "0"



