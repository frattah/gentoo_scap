# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low

- name: Ensure group owner on /etc/selinux/
  file:
    path: "/etc/selinux/"
    state: directory

    group: "root"



