# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low

- name: Ensure owner on directory /etc/sudoers.d/
  file:
    path: "/etc/sudoers.d/"
    state: directory

    owner: "0"



