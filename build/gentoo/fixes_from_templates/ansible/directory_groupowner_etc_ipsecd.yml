# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low

- name: Ensure group owner on /etc/ipsec.d/
  file:
    path: "/etc/ipsec.d/"
    state: directory

    group: "root"



