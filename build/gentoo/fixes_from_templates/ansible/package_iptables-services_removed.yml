# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low
- name: Ensure iptables-services is removed
  package:
    name: "iptables-services"
    state: absent
