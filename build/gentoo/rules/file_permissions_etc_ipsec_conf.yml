description: ' To properly set the permissions of <code>/etc/ipsec.conf</code>, run
    the command: <pre>$ sudo chmod 0644 /etc/ipsec.conf</pre>'
rationale: 'Setting correct permissions on the /etc/ipsec.conf file is important

    because this file hosts Libreswan configuration. Protection of this

    file is critical for system security. Restricting the permissions

    ensures exclusive control of the Libreswan configuration.'
severity: medium
references:
    anssi:
    - R50
components:
- libreswan
identifiers: {}
ocil_clause: /etc/ipsec.conf does not have unix mode 0644
ocil: 'To check the permissions of <code>/etc/ipsec.conf</code>,

    run the command:

    <pre>$ ls -l /etc/ipsec.conf</pre>

    If properly configured, the output should indicate the following permissions:

    <code>0644</code>'
oval_external_content: null
fixtext: ' Configure the "/etc/ipsec.conf" file to "0644" by running the following
    command:

    $ sudo chmod 0644 /etc/ipsec.conf'
checktext: ''
vuldiscussion: ''
srg_requirement: ' The Gentoo /etc/ipsec.conf file must have mode 0644 or less permissive
    to prevent unauthorized access.'
warnings: []
conflicts: []
requires: []
policy_specific_content: {}
platform: package[libreswan]
platforms: !!set
    package[libreswan]: null
sce_metadata: {}
inherited_platforms: !!set {}
cpe_platform_names: !!set
    package_libreswan: null
inherited_cpe_platform_names: !!set {}
bash_conditional: null
fixes: {}
title: Verify Permissions On /etc/ipsec.conf File
definition_location: /home/fra/gentoo_scap/linux_os/guide/system/network/network-ipsec/file_permissions_etc_ipsec_conf/rule.yml
template:
    name: file_permissions
    vars:
        filepath: /etc/ipsec.conf
        filemode: '0644'
    backends: {}
documentation_complete: true
