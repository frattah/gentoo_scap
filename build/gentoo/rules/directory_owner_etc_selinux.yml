description: ' To properly set the owner of <code>/etc/selinux</code>, run the command:
    <pre>$ sudo chown root /etc/selinux </pre>'
rationale: 'The ownership of the /etc/selinux directory by the root user is important

    because this directory hosts SELinux configuration. Protection of this

    directory is critical for system security. Assigning the ownership to root

    ensures exclusive control of the SELinux configuration.'
severity: medium
references:
    anssi:
    - R50
components:
- selinux
identifiers: {}
ocil_clause: /etc/selinux does not have an owner of root
ocil: 'To check the ownership of <code>/etc/selinux</code>,

    run the command:

    <pre>$ ls -lL /etc/selinux</pre>

    If properly configured, the output should indicate the following owner:

    <code>root</code>'
oval_external_content: null
fixtext: ' Change the owner of the file /etc/selinux to root by running the following
    command:

    $ sudo chown root /etc/selinux'
checktext: ''
vuldiscussion: ''
srg_requirement: ' The Gentoo /etc/selinux file must be owned by root.'
warnings: []
conflicts: []
requires: []
policy_specific_content: {}
platform: null
platforms: !!set {}
sce_metadata: {}
inherited_platforms: !!set
    machine: null
cpe_platform_names: !!set {}
inherited_cpe_platform_names: !!set
    machine: null
bash_conditional: null
fixes: {}
title: Verify User Who Owns /etc/selinux Directory
definition_location: /home/fra/gentoo_scap/linux_os/guide/system/selinux/directory_owner_etc_selinux/rule.yml
template:
    name: file_owner
    vars:
        filepath: /etc/selinux/
        fileuid: '0'
    backends: {}
documentation_complete: true
