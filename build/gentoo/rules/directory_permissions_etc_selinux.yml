description: ' To properly set the permissions of <code>/etc/selinux</code>, run the
    command: <pre>$ sudo chmod 0755 /etc/selinux</pre>'
rationale: 'Setting correct permissions on the /etc/selinux directory is important

    because this directory hosts SELinux configuration. Protection of this

    directory is critical for system security. Restricting the permissions

    ensures exclusive control of the SELinux configuration.'
severity: medium
references:
    anssi:
    - R50
components:
- selinux
identifiers: {}
ocil_clause: /etc/selinux does not have unix mode 0755
ocil: 'To check the permissions of <code>/etc/selinux</code>,

    run the command:

    <pre>$ ls -l /etc/selinux</pre>

    If properly configured, the output should indicate the following permissions:

    <code>0755</code>'
oval_external_content: null
fixtext: ' Configure the "/etc/selinux" file to "0755" by running the following command:

    $ sudo chmod 0755 /etc/selinux'
checktext: ''
vuldiscussion: ''
srg_requirement: ' The Gentoo /etc/selinux file must have mode 0755 or less permissive
    to prevent unauthorized access.'
warnings: []
conflicts: []
requires: []
policy_specific_content: {}
platform: null
platforms: !!set {}
sce_metadata: {}
inherited_platforms: !!set
    machine: null
cpe_platform_names: !!set {}
inherited_cpe_platform_names: !!set
    machine: null
bash_conditional: null
fixes: {}
title: Verify Permissions On /etc/selinux Directory
definition_location: /home/frattah/old/linux_os/guide/system/selinux/directory_permissions_etc_selinux/rule.yml
template:
    name: file_permissions
    vars:
        filepath: /etc/selinux/
        filemode: '0755'
    backends: {}
documentation_complete: true
