description: ' To properly set the group owner of <code>/etc/selinux</code>, run the
    command: <pre>$ sudo chgrp root /etc/selinux</pre>'
rationale: 'The ownership of the /etc/selinux directory by the root group is important

    because this directory hosts SELinux configuration. Protection of this

    directory is critical for system security. Assigning the ownership to root

    ensures exclusive control of the SELinux configuration.'
severity: medium
references:
    anssi:
    - R50
components:
- selinux
identifiers: {}
ocil_clause: /etc/selinux does not have a group owner of root
ocil: 'To check the group ownership of <code>/etc/selinux</code>,

    run the command:

    <pre>$ ls -lL /etc/selinux</pre>

    If properly configured, the output should indicate the following group-owner:

    <code>root</code>'
oval_external_content: null
fixtext: ' Change the group of the file /etc/selinux to root by running the following
    command:

    $ sudo chgrp root /etc/selinux'
checktext: ''
vuldiscussion: ''
srg_requirement: ' The Gentoo /etc/selinux file must be group-owned by root.'
warnings: []
conflicts: []
requires: []
policy_specific_content: {}
platform: null
platforms: !!set {}
sce_metadata: {}
inherited_platforms: !!set
    machine: null
cpe_platform_names: !!set {}
inherited_cpe_platform_names: !!set
    machine: null
bash_conditional: null
fixes: {}
title: Verify Group Who Owns /etc/selinux Directory
definition_location: /home/frattah/old/linux_os/guide/system/selinux/directory_groupowner_etc_selinux/rule.yml
template:
    name: file_groupowner
    vars:
        filepath: /etc/selinux/
        gid_or_name: root
    backends: {}
documentation_complete: true
